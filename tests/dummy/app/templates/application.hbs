{{todo-list
  onToggle=(action onToggle)
  todos=model
}}

Completed {{completedTodos.length}} todos

{{#react-component "the-wrapper"}}
  <button type='button' {{action 'onClick'}}>Test</button>
  {{~value~}}
{{/react-component}}