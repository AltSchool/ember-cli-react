version: 2

jobs:
  build:
    docker:
      - image: circleci/node:6-browsers
    steps:
      - checkout
      - run:
          name: Install Packages
          command: npm install
      - run:
          name: Run Tests
          command: npm test
